#para configurar a base de dados
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

#para saber o dialeto do banco de dados
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#habilitando o console do H2 database
spring.h2.console.enabled=true
#url para pode acessar o H2
spring.h2.console.path=/h2-console

#tabela criada para demonstra��o
#create table cliente(
#id int not null primary key,
#nome varchar(100)
#)

#Mostrando o sql da consulto do JPA
#Essa propriedade somente mostra
spring.jpa.properties.hibernate.show_sql=true
#Essa formata para ficar bonitinho no console
spring.jpa.properties.hibernate.format_sql=true